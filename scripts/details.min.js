let c=console.log;const queryString=location.search,params=new URLSearchParams(queryString),id=params.get("id");c("id: "+id),c("todas las cards: "+data.currentDate);const categorias=data.events.find(a=>a._id==id);c(JSON.stringify(categorias)),c(categorias._id);let container=document.querySelector(".main-container__details"),plantillaDetalles="";function Detalles(){plantillaDetalles+=`
    <div class="imagen-detail">
        <img src="${categorias.image}" alt="imagen">
    </div>

    <div class="texto-detail">
        <div class="textos">
            <h1>${categorias.name}</h1>
            <p>${categorias.description}</p>
        </div>
    </div>
    `,container.innerHTML=plantillaDetalles}Detalles();